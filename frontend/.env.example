# Copy to .env.local for local overrides, or set these in your host's
# (Cloudflare Pages / Vercel / Netlify) environment variable settings.
# With neither set, the app defaults to localhost for local dev.

VITE_API_BASE=https://your-backend.onrender.com/api
VITE_REALTIME_WS_URL=wss://troid-realtime.your-subdomain.workers.dev/ws
